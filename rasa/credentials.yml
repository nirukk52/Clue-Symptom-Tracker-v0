# Rasa Credentials Configuration
#
# Why this exists: Configures input channels for Rasa.
# We only use the REST API — Next.js calls Rasa via HTTP.

# REST API channel (our primary interface)
rest:

# Rasa X / Rasa Pro (disabled for OSS)
# rasa:
#   url: "http://localhost:5002/api"
