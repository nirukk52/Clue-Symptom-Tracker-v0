# Rasa Endpoints Configuration
#
# Why this exists: Configures Redis tracker store for state persistence.
# Dialogue state survives server restarts.

tracker_store:
  type: redis
  url: localhost
  port: 6380
  db: 1
  use_ssl: false
  record_exp: 86400
